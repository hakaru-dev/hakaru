fn noiseTprev prob:
 fn noiseMprev prob:
  noiseTprime <~ uniform(3,8)
  noiseMprime <~ uniform(1,4)
  weight(1/2, return (real2prob(noiseTprime), noiseMprev)) <|>
  weight(1/2, return (noiseTprev, real2prob(noiseMprime)))