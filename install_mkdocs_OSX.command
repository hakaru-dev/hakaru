#!/bin/bash
# You must have Pip installed to use this script. To install Pip, you must have Python installed
# on your system. You can check what version of Python you have by using the command:
#   python --version
# If a Python installation is found, you can install Pip using:
#   sudo easy_install pip
pip install mkdocs
pip install python-markdown-math
pip install mkdocs-extensions
pip install mkdocs-bootswatch